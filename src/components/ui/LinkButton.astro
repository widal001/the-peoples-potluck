---
interface Props {
  href: string;
  text: string;
  class?: string;
}

const { href, text, class: className = "" } = Astro.props;
---

<a href={href} class={`button-primary ${className}`}>
  {text}
</a>
